//this link executed into the railway site to run this program 
giving thiis error
create table playlist (
        id bigint generated by default as identity,
        album_name varchar(255),
        singer_name varchar(255),
        song_name varchar(255),
        primary key (id)
    )" via JDBC [You have an error in your SQL syntax; check the manual that corresponds to your MySQL server version for the right syntax to use near 'by default as identity,
        album_name varchar(255),
        singer_name v' at line 2]

org.hibernate.tool.schema.spi.CommandAcceptanceException: Error executing DDL "
    create table playlist (
        id bigint generated by default as identity,
        album_name varchar(255),
        singer_name varchar(255),
        song_name varchar(255),
        primary key (id)
    )" via JDBC [You have an error in your SQL syntax; check the manual that corresponds to your MySQL server version for the right syntax to use near 'by default as identity,
        album_name varchar(255),
        singer_name v' at line 2]
        
        



CREATE TABLE playlist (
    id BIGINT AUTO_INCREMENT,
    album_name VARCHAR(255),
    singer_name VARCHAR(255),
    song_name VARCHAR(255),
    PRIMARY KEY (id)
);
